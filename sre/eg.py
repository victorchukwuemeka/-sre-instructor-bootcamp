#check the packages if they still have support 
# if out of support 
# create an update query 


import sqlite 

directory = "packages" 
if directory :
    check(directory)



def check(directory):
    import datetime 
    datetime == directory.datetime
    call_sql()


def call_sgl()
    q = "UPDATE   custome WHERE status=(?),(Error)"
    conn = sqlite.conection()
    cur = conn.cursor()
    ql = cur.execute(q)
    call_log(ql)




unit
